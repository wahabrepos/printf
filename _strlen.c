#include "main.h"
/**
 * _strlen - count the length of a string
 * @s: string
 * Return: the length of a string
 */
int _strlen(char *s)
{
	int count;

	for (count = 0; s[count] != 0; count++)
	{
	}
	return (count);
}
